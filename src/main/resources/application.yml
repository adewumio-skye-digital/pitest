spring:
  jpa.hibernate.ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/expensis
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: categories
